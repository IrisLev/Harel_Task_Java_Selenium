package pages;

import org.openqa.selenium.WebDriver;

public class DatesPage {

    private final WebDriver driver;

    public DatesPage(WebDriver driver) {
        this.driver = driver;
    }

    // You can add methods for date picking later
}
